Use external libraries in RCP e4 application
--------------------------------------------

This is essentially a reference folder. The libraries really used (gdal for instance) are 
located in the OS-specific fragments and declared in the MANIFEST file (MANIFEST.MF section).
